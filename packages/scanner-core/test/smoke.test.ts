/**
 * Simple smoke test for scanner-core package.
 */

import { describe, expect, it } from 'vitest';

describe('scanner-core package bootstrap', () => {
  it('runs a placeholder test', () => {
    expect(1 + 1).toBe(2);
  });
});